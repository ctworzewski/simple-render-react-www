# simple-render-react-www

## Tworzę elementy  `header`, `main` oraz `footer`, które służą jako moja strona internetowa, następnie wstrzykuje te elementy do DOM'a - wyniku czego powstaje prosta strona internetowa.